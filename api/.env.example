# Supabase (server-side — use service_role key, never anon key)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Set by Railway automatically — do not define manually
# PORT=8000
