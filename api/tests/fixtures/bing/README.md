# Bing WMT fixtures (EXAMPLE / MOCK data)

All JSON files in this directory are SYNTHETIC fixtures crafted for
testing api/jobs/bing_ingest.py. They do not contain real Bing data
and the queries / pages are fictional. Do not interpret them as
representative of any real site.

Follows the labelling convention from docs/policies/conventions.md.
