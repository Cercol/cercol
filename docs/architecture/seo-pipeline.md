# SEO data pipeline

Stub. Full content lands in FASE G of Phase 17.6.1a.

Will cover: GCP project layout, the two BigQuery datasets
(`searchconsole` bulk export, `cercol_seo` ours), the three ingest
jobs (Bing, PageSpeed, Caddy log parser), the cron-based scheduling
pattern, and the structural limitation that frontend cercol.team has
no origin logs (only the API surface does).
