# SEO BigQuery schemas

Stub. Full content lands in FASE G of Phase 17.6.1a.

Will cover: every column in `cercol_seo.bing_query_stats`,
`bing_page_stats`, `bing_crawl_stats`, `pagespeed_runs`, `crawl_logs`.
Partition keys, clustering, retention.
